// Nexa Commerce auth service.
// Standard library only - no third-party modules, so the SCA surface for
// this service is empty and no transitive CVE can raise a finding here.
module github.com/nexa-commerce/auth-service

go 1.22
